/// <reference types="vite/client" />

// Injected by Vite's `define` block from package.json — see vite.config.ts.
// Mirrors how Tauri's own templates declare build-time constants.
declare const __APP_VERSION__: string;
